# Base Go API Engine

[![Go Version](https://img.shields.io/badge/Go-1.22+-00ADD8?style=flat&logo=go)](https://golang.org/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16.3-316192?style=flat&logo=postgresql)](https://www.postgresql.org/)
[![License](https://img.shields.io/badge/License-Free_to_Use-green.svg)](LICENSE)
[![Maintenance](https://img.shields.io/badge/Maintained%3F-yes-green.svg)](https://github.com/dimbo1324)

> **Надежная, модульная и масштабируемая основа для создания RESTful API на Go (Golang) и PostgreSQL.**

---

### 🌐 Документация на других языках

* 🇺🇸 [**English (Английский)**](../Readme.md)
* 🇪🇸 [**Spanish (Испанский)**](ReadmeSp.md)

---

## 📖 Обзор

**Base Go API Engine** — это готовый к использованию шаблон (boilerplate), предназначенный для быстрого старта разработки бэкенда. Проект реализует паттерн «чистой архитектуры» (Clean Architecture), разделяя логику конфигурации, работу с базой данных и бизнес-правила.

Независимо от того, изучаете ли вы Go или создаете сложный микросервис, этот движок предоставляет всю необходимую базу — подключение к БД, управление конфигурацией и маршрутизацию — позволяя вам сосредоточиться на реализации функций продукта.

### ✨ Ключевые особенности

* **Модульная архитектура:** Четкое разделение пакетов `cmd`, `internal` и `components`
* **Производительный роутинг:** Использование `chi` v5 для легковесной и идиоматичной маршрутизации
* **Интеграция с PostgreSQL:** Преднастроенный пул соединений и поддержка расширения `citext`
* **Готовность к Docker:** Включает `docker-compose.yml` для мгновенного развертывания базы данных
* **Умная конфигурация:** Надежное управление переменными окружения с разумными значениями по умолчанию
* **Масштабируемое хранилище:** Готовые интерфейсы для управления пользователями (Users) и постами (Posts)

---

## 🛠️ Стек технологий

* **Язык:** [Go (Golang)](https://go.dev/)
* **База данных:** [PostgreSQL](https://www.postgresql.org/)
* **Роутер:** [go-chi/chi](https://github.com/go-chi/chi)
* **Драйвер:** [lib/pq](https://github.com/lib/pq)
* **Контейнеризация:** Docker и Docker Compose

---

## 🚀 Начало работы

Следуйте этим шагам, чтобы запустить локальную копию проекта на вашем компьютере.

### Предварительные требования

* **Go**: Версия 1.22 или выше.
* **Docker Desktop**: Для запуска контейнера с базой данных.
* **Git**: Для клонирования репозитория.

### 1. Клонирование репозитория

```bash
git clone [https://github.com/dimbo1324/Base-Go-API-Engine.git](https://github.com/dimbo1324/Base-Go-API-Engine.git)
cd Base-Go-API-Engine

```

### 2. Конфигурация окружения

Приложение спроектировано для работы «из коробки» со значениями по умолчанию, но вы можете настроить его через переменные окружения.

| Переменная              | Описание                  | Значение по умолчанию                             |
| ----------------------- | ------------------------- | ------------------------------------------------- |
| `ADDR`                  | Адрес сервера             | `:8080`                                           |
| `DB_ADDR`               | Строка подключения к БД   | `postgres://postgres:password@localhost/appdb...` |
| `DB_MAX_OPEN_CONNS`     | Макс. открытых соединений | `100`                                             |
| `DB_MAX_IDLE_CONNS`     | Макс. простаивающих соед. | `100`                                             |
| `DB_MAX_IDLE_TIME_MINS` | Время жизни соединения    | `15m`                                             |

### 3. Запуск базы данных

Мы используем Docker Compose для поднятия экземпляра PostgreSQL с правильными настройками.

```bash
docker-compose up -d

```

Эта команда запустит контейнер PostgreSQL с именем `postgres-db` на порту `5432`.

### 4. Миграция базы данных

Проект содержит SQL-скрипты миграций в папке `cmd/migrate/migrations`. Их необходимо применить для создания таблиц `users` и `posts`.

Вы можете выполнить SQL-файлы через любой клиент БД (например, DBeaver) или через командную строку:

```bash
# Пример использования psql внутри контейнера
docker exec -it postgres-db psql -U postgres -d appdb -f /path/to/000001_create_users.up.sql

```

### 5. Запуск приложения

```bash
go run cmd/api/main.go

```

Вы должны увидеть вывод в консоли:

```text
Сервер запущен на :8080

```

(Сообщение о запуске на русском языке уже встроено в код )

---

## 📂 Структура проекта

Проект следует стандарту Standard Go Project Layout:

```text
Base-Go-API-Engine/
├── cmd/
│   ├── api/            # Точка входа в приложение (main.go)
│   └── migrate/        # SQL скрипты миграций
├── internal/
│   ├── config/         # Константы конфигурации и дефолтные значения
│   ├── db/             # Логика подключения к базе данных
│   ├── env/            # Помощники для работы с переменными окружения
│   └── store/          # Слой доступа к данным (Репозитории)
├── docker-compose.yml  # Описание сервисов Docker
└── go.mod              # Зависимости Go модуля

```

---

## 🔌 API Эндпоинты

### Система

| Метод | Эндпоинт     | Описание                                       |
| ----- | ------------ | ---------------------------------------------- |
| `GET` | `/v1/status` | Проверка работоспособности (Health check) API. |

 |

Примечание: Логика работы с пользователями и постами реализована в пакете `internal/store` и готова к подключению к новым HTTP-хендлерам.

---

## 🤝 Вклад в развитие (Contributing)

Ваш вклад делает сообщество Open Source потрясающим местом для обучения, вдохновения и творчества. Любая помощь **высоко ценится**.

1. Форкните проект (Fork).
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`).
3. Закоммитьте изменения (`git commit -m 'Добавил AmazingFeature'`).
4. Запушьте ветку (`git push origin feature/AmazingFeature`).
5. Откройте Pull Request.

---

## 📜 Лицензия

Этот проект свободен для использования. Чувствуйте себя свободно, используя его в своих разработках.

---

## 📬 Контакты

Если у вас есть вопросы, предложения или вы просто хотите поздороваться, пишите!

* **Автор:** dimbo1324
* **Telegram:** [@dimbo1324](https://t.me/dimbo1324)
* **Email:** dimaprihodko180@gmail.com
* **GitHub:** [github.com/dimbo1324](https://github.com/dimbo1324)

---

*Разработано с ❤️ by dimbo1324*
