# Base Go API Engine

Надежная, модульная и масштабируемая основа для создания RESTful API с использованием **Go (Golang)** и **PostgreSQL**. Этот проект реализует паттерн чистой архитектуры, разделяя конфигурацию, хранилище базы данных и слои HTTP-транспорта.

---

### 🌐 Переводы документации

* [🇺🇸 English (Английский)](../Readme.md)
* [🇪🇸 Español (Испанский)](ReadmeSp.md)

---

## ✨ Особенности

* **Модульная архитектура**: Чистое разделение ответственности (CMD, Internal, Components, Store).
* **Высокопроизводительная маршрутизация**: Построена на [chi v5](https://github.com/go-chi/chi), легковесном и идиоматичном роутере.
* **Управление базой данных**:
* Интеграция PostgreSQL 16.3 через Docker.
* Конфигурация пула соединений (Max Open/Idle connections, Idle time).
* Поддержка расширения `citext` для регистронезависимой обработки email.




* **Стек Middleware**:
* Тегирование ID запроса (Request ID).
* Определение реального IP.
* Структурированное логирование.
* Восстановление после паники (Panic Recovery).
* Тайм-ауты (глобально 60 с).




* 
**Конфигурация окружения**: Гибкое управление конфигурацией со значениями по умолчанию.


* **Модели данных**:
* 
**Users**: Логика хранения для управления пользователями (Email, Имя пользователя, Пароль).


* 
**Posts**: Логика хранения для постов блога с поддержкой тегов.





## 🛠 Технологический стек

* **Язык**: Go 1.22+
* 
**База данных**: PostgreSQL 16.3 


* 
**Роутер**: go-chi/chi/v5 


* 
**Драйвер**: lib/pq 


* 
**Контейнеризация**: Docker и Docker Compose 



## 🚀 Начало работы

### Предварительные требования

* **Go** (установлена версия 1.21 или выше)
* **Docker** и **Docker Compose**

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/dimbo1324/base-go-api-t.git
cd base-go-api-t

```


2. **Запустите базу данных:**
Используйте Docker Compose для запуска контейнера PostgreSQL.
```bash
docker-compose up -d

```


Это запустит контейнер Postgres с именем `postgres-db` на порту `5432` с постоянным томом.


3. **Запустите миграции:**
*Убедитесь, что база данных инициализирована схемами из `cmd/migrate/migrations`.*
Проект включает SQL-скрипты для создания таблиц `users` и `posts`.


4. **Запустите приложение:**
```bash
go run cmd/api/main.go

```


Сервер запустится по адресу, указанному в вашей конфигурации (по умолчанию: `:8080`).



## ⚙️ Конфигурация

Приложение использует переменные окружения для конфигурации. Если переменные не установлены, используются жестко заданные значения по умолчанию, определенные в `internal/config/config.go`.

| Переменная              | Значение по умолчанию | Описание                                 |
| ----------------------- | --------------------- | ---------------------------------------- |
| `ADDR`                  | `:8080`               | Адрес прослушивания сервера              |
| `DB_ADDR`               | `postgres://...`      | Строка подключения к базе данных         |
| `DB_MAX_OPEN_CONNS`     | `30`                  | Макс. кол-во открытых соединений БД      |
| `DB_MAX_IDLE_CONNS`     | `30`                  | Макс. кол-во простаивающих соединений БД |
| `DB_MAX_IDLE_TIME_MINS` | `15m`                 | Макс. время простоя соединения           |

## 📂 Структура проекта

```text
base-go-api-t/
├── cmd/
│   ├── api/
│   │   ├── components/     # Структура App, монтирование роутера, обработчики
│   │   └── main.go         # Точка входа, внедрение зависимостей
│   └── migrate/            # Файлы SQL миграций
├── internal/
│   ├── config/             # Константы конфигурации и ключи
│   ├── db/                 # Низкоуровневая логика подключения к БД
│   ├── env/                # Вспомогательные функции для чтения env переменных
│   └── store/              # Реализация паттерна репозиторий (Users, Posts)
├── scripts/
│   └── initDb.sql          # Скрипт инициализации базы данных
├── docker-compose.yml      # Определение инфраструктуры Docker
└── go.mod                  # Определения модулей Go

```

## 📡 API Эндпоинты

### Система

* `GET /v1/status`: Эндпоинт проверки работоспособности. Возвращает "OK: it works".



*(Примечание: Хотя слой хранения поддерживает создание пользователей и постов, конкретные HTTP-обработчики для этих сущностей должны быть зарегистрированы в `cmd/api/components/methods.go`).*

## 📄 Лицензия

Этот проект является проектом с открытым исходным кодом.